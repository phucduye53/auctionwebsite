@model IEnumerable<auctionwebsite.Models.Product>
@{
    ViewBag.Title = "Sản phẩm đang theo dõi";
    Layout = "~/Views/Shared/user_layout.cshtml";
}

<!DOCTYPE html>
<div class="span9">
    <ul class="breadcrumb">
        <li><a href="index.html">Trang chủ</a> <span class="divider">/</span></li>
        <li><a href="products.html">Tài khoản</a> <span class="divider">/</span></li>
        <li class="active">Sản phẩm đang theo dõi</li>
    </ul>
    </ul>
    <h3>Sản phẩm đang theo dõi[<small>@Model.Count() sản phẩm</small>]</h3>
    <hr class="soft" />
    <table id="myTable" class="table table-bordered">
        <thead>
            <tr>
                <th>STT</th>
                <th>Sản phẩm</th>
                <th>Chi tiết</th>
                <th>Người mua giá cao nhất</th>
                <th>Giá hiện tại</th>
                <th>Được đăng bởi</th>
                <th>Trạng thái</th>
                <th>Hủy theo dõi</th>
            </tr>
        </thead>
        <tbody>
            @{
                var stt = 1;
           }
            @foreach (var item in Model)
            {
                <tr>
                    <td>@stt</td>
                    <td><a href=""> <img width="60" src="~/Img/@(item.ProductID+"_"+item.ProductPicName)" alt="" /></a></td>
                    <td>@item.ProductDes<br /></td>
                    <td>0</td> @*user highest price buy*@
                    <td>$120.00</td> @*highest price*@
                    <td>@item.User.UserName</td>
                    @if (@item.ProductStatus == 0)
                    {
                        <td>Đang chờ xác nhận</td>
                    }
                    @if (@item.ProductStatus == 1)
                    {
                        <td>Đang đấu giá</td>
                    }
                    @if (@item.ProductStatus == 2)
                    {
                        <td>Đấu giá thành công</td>
                    }
                    @if (@item.ProductStatus == 3)
                    {
                        <td>Đấu giá đã hủy</td>
                    }
                    <td><button class="alert-danger" id="deleteProduct" data-id="@item.ProductID"><i class="icon-remove icon-white"></i></button></td>                  
                </tr>
                stt++;
            }
        </tbody>
    </table>
</div>
</div></div>
</div>
@section Scripts{
    <script type="text/javascript">
        $('.alert-danger').click(function () {
            var row = $(this).closest('tr').index();
            if (confirm('Bạn có muốn xóa khỏi danh sách theo dõi ?')) {
                $.ajax({
                    type: 'POST',
                    url: '@Url.Action("DeleteFavProduct")',
                    dataType: 'json',
                    data: { id: $(this).data('id') },
                    success: function (data) {
                        document.getElementById("myTable").deleteRow(row+1);
                    },
                    error: function (ex) {
                        alert("Fail" + ex);
                    }
                });
                return false;
            }
        });
    </script>
}