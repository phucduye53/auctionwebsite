@model IEnumerable<auctionwebsite.Models.Cateparent>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/admin_layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CateparentName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CateparentName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CateparentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CateparentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CateparentID })
        </td>
    </tr>
}

</table>
